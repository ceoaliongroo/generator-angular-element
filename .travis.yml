language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-yeoman-generator-angular-components' ]; then cd .. && eval "mv $currentfolder generator-yeoman-generator-angular-components" && cd generator-yeoman-generator-angular-components; fi

